server.port=${PORT:8080}

# Database Configuration
spring.datasource.url=jdbc:h2:file:./data/loan-db;DB_CLOSE_DELAY=-1;DATABASE_TO_UPPER=false
spring.datasource.username=sa
spring.datasource.password=
spring.datasource.driver-class-name=org.h2.Driver

# Hibernate Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.H2Dialect

# H2 Console (disabled in production)
spring.h2.console.enabled=true
spring.h2.console.path=/h2-console

# CORS - allow all origins (frontend handles this)
spring.mvc.cors.allowed-origins=*
spring.mvc.cors.allowed-methods=GET,POST,PUT,DELETE,OPTIONS
spring.mvc.cors.allow-credentials=false

# Email SMTP Configuration (use environment variables in production)
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=${MAIL_USERNAME:naikomkar5428@gmail.com}
spring.mail.password=${MAIL_PASSWORD:edkkcatfnacblbcz}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true



